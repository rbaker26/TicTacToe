language: java
jdk:
  - oraclejdk11
os:
  - linux

branches:
  only:
    - master

script:
  - chmod +x ./gradlew
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
  - ./gradlew check
  - ./gradlew test
  - ./gradlew run
